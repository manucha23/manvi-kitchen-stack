{
  "name": "order-list",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "aws-sdk": "2.1430.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^20.5.1",
    "@types/aws-sdk": "^2.7.0"
  }
}
